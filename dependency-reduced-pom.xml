<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.guoqinlong</groupId>
  <artifactId>jettyserver</artifactId>
  <version>1.0-SNAPSHOT</version>
  <build>
    <finalName>${build.artifact.name}</finalName>
    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${maven.compiler.plugin.version}</version>
        <configuration>
          <source>${java.source}</source>
          <target>${java.target}</target>
          <showDeprecation>true</showDeprecation>
          <showWarnings>true</showWarnings>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-jar-plugin</artifactId>
        <version>2.4</version>
        <configuration>
          <archive>
            <manifest>
              <mainClass>${main.class.name}</mainClass>
            </manifest>
          </archive>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>${shade.version}</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <transformers>
                <transformer>
                  <mainClass>${main.class.name}</mainClass>
                </transformer>
              </transformers>
            </configuration>
          </execution>
        </executions>
        <configuration>
          <filters>
            <filter>
              <artifact>*:*</artifact>
              <excludes>
                <exclude>META-INF/*.SF</exclude>
                <exclude>META-INF/*.DSA</exclude>
                <exclude>META-INF/*.RSA</exclude>
              </excludes>
            </filter>
          </filters>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <properties>
    <build.artifact.name>jettyserver</build.artifact.name>
    <jersey.version>2.15</jersey.version>
    <jetty.version>9.3.1.v20150714</jetty.version>
    <commons.collections.version>4.0</commons.collections.version>
    <java.target>1.8</java.target>
    <main.class.name>com.qinlong.JettyServer</main.class.name>
    <shade.version>2.3</shade.version>
    <dependency.locations.enabled>false</dependency.locations.enabled>
    <hulu.java.commons.version>1.7.47</hulu.java.commons.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <java.source>1.8</java.source>
    <maven.compiler.plugin.version>3.1</maven.compiler.plugin.version>
    <commons.lang.version>3.3.2</commons.lang.version>
  </properties>
</project>

